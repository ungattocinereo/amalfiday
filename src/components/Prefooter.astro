---
---
<section class="prefooter">
  <div class="container prefooter__inner">
    <div class="prefooter__copy">
      <span class="prefooter__label">What we do for you</span>
      <h2>Your Amalfi, <span class="accent-italic">crafted</span></h2>

      <ul class="prefooter__services">
        <li>
          <i class="bi bi-camera-reels prefooter__ico prefooter__ico--sunset"></i>
          <div>
            <strong>Photo Sessions</strong>
            <span>Editorial portraits at golden hour. Cinematic frames, not tourist snapshots.</span>
          </div>
        </li>
        <li>
          <i class="bi bi-stars prefooter__ico prefooter__ico--azure"></i>
          <div>
            <strong>Curated Experiences</strong>
            <span>Private tours by car, boat, or motorbike. Wine tastings, hidden viewpoints, local stories.</span>
          </div>
        </li>
        <li>
          <i class="bi bi-house-heart prefooter__ico prefooter__ico--warm"></i>
          <div>
            <strong>Accommodation</strong>
            <span>Our apartments in Atrani, steps from the sea. Book direct or via Airbnb.</span>
          </div>
        </li>
      </ul>

      <div class="prefooter__actions">
        <a class="button" href="/contact">
          <i class="bi bi-send"></i> Send a request
        </a>
        <a class="prefooter__link" href="/experience">
          Browse all experiences <i class="bi bi-arrow-right"></i>
        </a>
      </div>
    </div>

    <div class="prefooter__media">
      <div class="prefooter__stack">
        <img
          class="prefooter__photo"
          src="/pre-footer/boat-tours-in-amalfi.webp"
          alt="Boat tours in Amalfi Coast"
          loading="lazy"
        />
        <img
          class="prefooter__photo"
          src="/pre-footer/girl-on-moto-in-amalfi-small.webp"
          alt="Amalfi scooter ride"
          loading="lazy"
        />
        <img
          class="prefooter__photo"
          src="/pre-footer/2-food-in-amalfi-good.webp"
          alt="Amalfi food experience"
          loading="lazy"
        />
      </div>
      <div class="prefooter__newsletter" id="newsletter">
        <p class="newsletter__cta">
          <i class="bi bi-envelope"></i>
          Subscribe and stay up to date with Amalfi Coast news and our latest offers
        </p>
        <form class="newsletter__form" id="newsletter-form" novalidate>
          <div class="newsletter__fields">
            <input
              type="email"
              class="newsletter__input"
              id="newsletter-email"
              placeholder="your@email.com"
              required
              autocomplete="email"
              aria-label="Email address"
            />
            <!-- Honeypot — hidden from humans, bots fill it -->
            <input type="text" name="website" class="newsletter__hp" tabindex="-1" autocomplete="off" aria-hidden="true" />
            <button class="button secondary newsletter__btn" type="submit">Subscribe</button>
          </div>
          <p class="newsletter__consent">
            Iscrivendoti, acconsenti al trattamento dei dati.
            <a href="/privacy">Privacy Policy</a>
          </p>
          <span class="newsletter__error" id="newsletter-error" role="alert"></span>
        </form>
        <div class="newsletter__success" id="newsletter-success" aria-live="polite">
          <svg class="newsletter__check" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg>
          <span>Thank you for subscribing</span>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .prefooter {
    padding: 4rem 0 5rem;
    background: var(--color-bg);
  }

  .prefooter__inner {
    display: grid;
    grid-template-columns: 1.2fr 0.8fr;
    gap: 3rem;
    align-items: start;
  }

  /* ── Left column ── */
  .prefooter__label {
    display: inline-block;
    font-family: var(--font-mono);
    font-size: 0.65rem;
    text-transform: uppercase;
    letter-spacing: 0.25em;
    color: var(--color-accent);
    margin-bottom: 0.6rem;
  }

  .prefooter__copy h2 {
    font-size: clamp(1.8rem, 3vw, 2.6rem);
    margin-bottom: 1.8rem;
  }

  /* ── Compact service list ── */
  .prefooter__services {
    list-style: none;
    margin: 0 0 2rem;
    padding: 0;
    display: flex;
    flex-direction: column;
    gap: 1.1rem;
  }

  .prefooter__services li {
    display: flex;
    align-items: flex-start;
    gap: 0.9rem;
  }

  .prefooter__ico {
    flex-shrink: 0;
    width: 36px;
    height: 36px;
    border-radius: 10px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.05rem;
    margin-top: 1px;
  }

  .prefooter__ico--sunset {
    background: var(--gradient-sunset);
    color: #ffffff;
  }

  .prefooter__ico--azure {
    background: var(--gradient-azure);
    color: #ffffff;
  }

  .prefooter__ico--warm {
    background: linear-gradient(135deg, #ffc300 0%, #ff5900 100%);
    color: #ffffff;
  }

  .prefooter__services strong {
    display: block;
    font-family: var(--font-display);
    font-size: 0.95rem;
    margin-bottom: 0.1rem;
  }

  .prefooter__services span {
    font-size: 0.85rem;
    line-height: 1.5;
    color: var(--color-text-muted);
  }

  /* ── Actions ── */
  .prefooter__actions {
    display: flex;
    gap: 1.2rem;
    align-items: center;
    flex-wrap: wrap;
  }

  .prefooter__link {
    font-weight: 600;
    color: var(--color-text);
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    transition: color 0.2s ease, gap 0.2s ease;
  }

  .prefooter__link:hover {
    color: var(--color-accent);
    gap: 0.6rem;
    text-decoration: none;
  }

  .prefooter__link i {
    font-size: 0.85em;
    transition: transform 0.2s ease;
  }

  .prefooter__link:hover i {
    transform: translateX(3px);
  }

  /* ── Right column ── */
  .prefooter__media {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
  }

  .prefooter__stack {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 0.8rem;
  }

  .prefooter__photo {
    height: 120px;
    border-radius: 12px;
    width: 100%;
    object-fit: cover;
    display: block;
    transition: transform 0.3s ease;
  }

  .prefooter__photo:hover {
    transform: scale(1.04);
  }

  /* ── Newsletter ── */
  .newsletter__cta {
    display: flex;
    align-items: flex-start;
    gap: 0.55rem;
    font-size: 0.95rem;
    line-height: 1.45;
    color: var(--color-text-muted);
    margin: 0 0 0.8rem;
  }

  .newsletter__cta i {
    color: var(--color-accent);
    font-size: 1.1rem;
    flex-shrink: 0;
    margin-top: 2px;
  }

  .prefooter__newsletter {
    padding: 1.2rem 1.4rem 0.6rem;
    border-radius: 16px;
    background: var(--color-surface);
  }

  .newsletter__form {
    display: flex;
    flex-direction: column;
    gap: 0.6rem;
  }

  .newsletter__label {
    font-family: var(--font-mono);
    font-size: 0.65rem;
    text-transform: uppercase;
    letter-spacing: 0.3em;
    color: var(--color-text-muted);
    text-align: center;
  }

  .newsletter__fields {
    display: flex;
    gap: 0.5rem;
    align-items: center;
  }

  .newsletter__input {
    flex: 1;
    min-width: 0;
    padding: 0.7rem 1rem;
    border: 1px solid var(--color-border);
    border-radius: 999px;
    background: var(--color-bg);
    color: var(--color-text);
    font-size: 0.9rem;
    font-family: var(--font-body);
    outline: none;
    transition: border-color 0.2s ease;
  }

  .newsletter__input:focus {
    border-color: var(--color-aux-2);
  }

  .newsletter__input.is-invalid {
    border-color: #e53e3e;
  }

  .newsletter__hp {
    position: absolute;
    left: -9999px;
    opacity: 0;
    height: 0;
    width: 0;
    pointer-events: none;
  }

  .newsletter__btn {
    padding: 0.7rem 1.2rem;
    font-size: 0.9rem;
    white-space: nowrap;
    flex-shrink: 0;
  }

  .newsletter__btn:disabled {
    opacity: 0.7;
    cursor: not-allowed;
    transform: none;
  }

  .newsletter__consent {
    margin: 0.25rem 0 0;
    font-size: 0.73rem;
    color: var(--color-text-muted);
    line-height: 1.4;
    padding: 0 0.2rem;
  }

  .newsletter__consent a {
    color: var(--color-text-muted);
    text-decoration: underline;
    text-underline-offset: 2px;
  }

  .newsletter__consent a:hover {
    color: var(--color-accent);
  }

  .newsletter__error {
    font-size: 0.8rem;
    color: #e53e3e;
    min-height: 1.1em;
    padding-left: 0.2rem;
  }

  .newsletter__success {
    display: none;
    align-items: center;
    gap: 0.6rem;
    padding: 0.4rem 0;
    font-weight: 600;
    color: var(--color-text);
    animation: newsletter-fadein 0.5s ease;
  }

  .newsletter__success.is-visible {
    display: flex;
  }

  .newsletter__check {
    width: 22px;
    height: 22px;
    color: #38a169;
    flex-shrink: 0;
  }

  @keyframes newsletter-fadein {
    from { opacity: 0; transform: translateY(6px); }
    to { opacity: 1; transform: translateY(0); }
  }

  @media (max-width: 1000px) {
    .prefooter__inner {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 500px) {
    .newsletter__fields {
      flex-direction: column;
    }
    .newsletter__input,
    .newsletter__btn {
      width: 100%;
    }
  }
</style>

<script>
  const form = document.getElementById('newsletter-form');
  const input = document.getElementById('newsletter-email');
  const error = document.getElementById('newsletter-error');
  const success = document.getElementById('newsletter-success');
  const loadedAt = Date.now();

  if (form && input && error && success) {
    form.addEventListener('submit', async (e) => {
      e.preventDefault();

      const email = input.value.trim();
      const hp = form.querySelector('[name="website"]').value;
      const btn = form.querySelector('.newsletter__btn');

      // Clear previous error
      error.textContent = '';
      input.classList.remove('is-invalid');

      // Client-side validation
      const re = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
      if (!email) {
        error.textContent = 'Please enter your email address';
        input.classList.add('is-invalid');
        input.focus();
        return;
      }
      if (!re.test(email)) {
        error.textContent = 'Please enter a valid email address';
        input.classList.add('is-invalid');
        input.focus();
        return;
      }

      btn.disabled = true;
      btn.textContent = '...';

      try {
        const res = await fetch('/hooks/subscribe-amalfiday', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ email, hp, t: loadedAt }),
        });

        const data = await res.json().catch(() => ({}));

        if (res.ok && data.ok) {
          form.style.display = 'none';
          success.classList.add('is-visible');
        } else {
          error.textContent = data.error || 'Something went wrong. Please try again.';
          btn.disabled = false;
          btn.textContent = 'Subscribe';
        }
      } catch {
        error.textContent = 'Connection error. Please try again.';
        btn.disabled = false;
        btn.textContent = 'Subscribe';
      }
    });
  }
</script>
