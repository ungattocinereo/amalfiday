---
import { footerLinks, featuredPhotoshoots, socialLinks } from '../data/site'
---
<footer class="footer">
  <div class="container footer__inner">
    <div class="footer__brand">
      <img src="/brand/logo-dark.svg" alt="Amalfi.Day" />
      <div class="footer__social">
        {socialLinks
          .filter((item) => ['Instagram', 'Facebook', 'Twitter'].includes(item.label))
          .map((item) => (
            <a class="footer__social-link" href={item.href} target="_blank" rel="noreferrer">
              {item.label[0]}
            </a>
          ))}
      </div>
    </div>

    <div class="footer__column">
      {footerLinks.transport.map((item) => (
        <a
          class="footer__link"
          href={item.href}
          target={item.external ? '_blank' : undefined}
          rel={item.external ? 'noreferrer' : undefined}
        >
          <span class="footer__icon">↘</span>
          {item.label}
        </a>
      ))}
    </div>

    <div class="footer__column">
      {footerLinks.apartments.map((item) => (
        <a class="footer__link" href={item.href} target="_blank" rel="noreferrer">
          <span class="footer__icon">↗</span>
          {item.label}
        </a>
      ))}
    </div>

    <div class="footer__column">
      {footerLinks.info.map((item) => (
        <a class="footer__link" href={item.href}>
          <span class="footer__icon">✦</span>
          {item.label}
        </a>
      ))}
    </div>

    <div class="footer__column">
      {featuredPhotoshoots.map((item) => (
        <a class="footer__link" href={item.href}>
          <span class="footer__icon">✷</span>
          {item.title}
        </a>
      ))}
    </div>
  </div>
  <div class="footer__bottom">
    <div class="container">
      <span>
        © 2014-{new Date().getFullYear()} CrystalPont S.R.L. / P.IVA: 06863730650 /
        <a href="/privacy"> Privacy Policy</a>
      </span>
    </div>
  </div>
</footer>

<style>
  .footer {
    padding: 4rem 0 0;
    background: #ffffff;
    border-top: 1px solid var(--color-surface);
  }

  .footer__inner {
    display: grid;
    gap: 2.5rem;
    grid-template-columns: 1.2fr repeat(4, 1fr);
    align-items: start;
  }

  .footer__brand img {
    height: 56px;
    margin-bottom: 1.5rem;
  }

  .footer__social {
    display: flex;
    gap: 0.8rem;
  }

  .footer__social-link {
    width: 44px;
    height: 44px;
    border-radius: 50%;
    border: 1px solid rgba(26, 26, 26, 0.2);
    display: inline-flex;
    align-items: center;
    justify-content: center;
    font-weight: 600;
    color: var(--color-text);
  }

  .footer__column {
    display: flex;
    flex-direction: column;
    gap: 0.8rem;
  }

  .footer__link {
    display: flex;
    gap: 0.6rem;
    align-items: center;
    color: var(--color-text);
    font-size: 0.95rem;
    font-weight: 500;
    border-bottom: 1px solid rgba(26, 26, 26, 0.1);
    padding-bottom: 0.4rem;
  }

  .footer__icon {
    font-size: 0.85rem;
    color: var(--color-text-muted);
  }

  .footer__bottom {
    margin-top: 3rem;
    padding: 1.5rem 0;
    border-top: 1px solid var(--color-surface);
    color: var(--color-text-muted);
    font-size: 0.85rem;
  }

  .footer__bottom a {
    color: inherit;
  }

  @media (max-width: 1100px) {
    .footer__inner {
      grid-template-columns: 1fr 1fr;
    }
  }

  @media (max-width: 640px) {
    .footer__inner {
      grid-template-columns: 1fr;
    }
  }
</style>
