{
  "name": "amalfi-day",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "build:static": "astro build && node scripts/precompress.mjs",
    "preview": "astro preview",
    "optimize-images": "node scripts/optimize-images.mjs",
    "precompress": "node scripts/precompress.mjs"
  },
  "dependencies": {
    "@astrojs/mdx": "^4.0.0",
    "@astrojs/vercel": "^8.0.0",
    "astro": "^5.0.0",
    "bootstrap-icons": "^1.11.3"
  },
  "overrides": {
    "path-to-regexp": "6.3.0"
  },
  "devDependencies": {
    "sharp": "^0.33.0"
  }
}
